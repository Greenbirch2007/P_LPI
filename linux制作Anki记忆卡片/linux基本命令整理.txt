adduser	在系统中增加一个用户或组 adduser和addgroup根据命令行选项和/etc/adduser.conf中配置信息增加用户和组
alias	指定别名 BASH内置命令。不带参数或使用-p选项时以"alias name=value"格式将别名列表输出到stdin。当提供了参数时定义别名为给定的值。
apropos	查找手册页的名字和描述 每个手册页里都有一个简短的描述。apropos在这个描述中查找keyword。keyword是正则表达式，如果使用了选项-r，或者包含通配符(-w)，或文本(-e)。apropos使用的数据库通过mandb命令来更新。根据安装，这可能是一个定期的cron作业来更新，或在安装了新的手册页时需要手动执行更新。
awk	模式扫描和文本处理语言 mawk是AWK语言的解释器。AWK语言在处理数据文件，文本提取和处理非常有用。一个AWK程序是一系列的pattern {action}对和函数定义。
basename	去掉文件名的目录和后缀
bash	GNU Bourne-Again SHell Bash是一个命令语言解释器，执行从stdin或文件读取的命令。
bg	在后台恢复暂停的程序 BASH内置命令。在后台恢复暂停的作业jobspec。就像用&运行程序一样。如果不指定jobspec，shell则使用当前的作业。按CTRL+Z暂停程序。
bind	绑定键和功能或宏 显示当前readline的键和功能绑定，绑定键和readline的功能或宏，或设置一个readline变量。每个非选项参数是一个命令，它将出现在.inputrc。但每个绑定或命令必须作为一个分开参数传入。如'"\C-x\C-r": re-read-init-file'。
cal	显示日历 cal显示一个简单的日历，ncal有更多选项和显示复活节日期。如果不指定参数，显示当前月的日历。
calendar	提醒服务 calendar检查当前目录或CALENDAR_DIR环境变量指定的目录，查找名为calendar的文件，显示以今天或明天开始的行。在星期五，会显示从星期五到下星期一。
cat	合并文件并输出到stdout 合并文件或标准输入，输出到stdout。如果没有文件或文件为-，读取标准输入。
cd	更改目录 BASH内置命令。更改当前目录到dir，环境变量HOME是默认的目录。环境变量CDPATH定义搜索路径，在这些路径中查找包含dir的目录。CDPATH中的目录以:分隔。如果dir以/开始，则不会使用CDPATH。参数-等同于$OLDPWD。help cd显示帮助信息。
chattr	更改ext2文件系统的文件属性 change file attributes，mode的格式是+-=[acdijsuADST]。
chgrp	"更改文件所属的组 change group
，更改文件所属的组为GROUP，如果使用--reference，更改文件所属的组为文件RFILE所属的组。"
chmod	更改文件的访问权限 change mode，只有文件所有者和超级用户可以修改文件或目录的权限。
chown	更改文件所有者和所属组 如果只有所有者(用户名或数字的用户ID)，用户成为每个文件的所有者，文件的所属组不变。如果所有者后跟着:但组名为空，用户成为所有者，用户的登录组成为文件的所属组。如果有:和组名而没有所有者，只更改文件所属组。如果只有:或整个都是空的，所有者和所属组都不会改变。
cmp	比较两个文件 compare，比较两个任何类型的文件，并将结果写入stdout。如果文件内容相同，没有输出。如果内容有不同，输出第一个不同点的字节和行号。字节和行号从1开始编号。如果文件为'-'或没有，从标准输入中读取。
command	执行一个简单命令或显示关于命令的信息 运行带有参数的命令不会进行正常的shell功能查找。只有builtin命令或PATH中的命令会被执行。
cp	复制文件和目录 复制SOURCE到DEST，或多个SOURCE到DIRECTORY。
cpio	复制文件到或从档案
cscope	开发者浏览代码的工具 cscope默认处理当前目录下的C(.c和.h)，lex(.l)，yacc(.y)源文件。也可以处理命令行上指定的源文件。cscope在标准目录中查找#include文件如果在当前没有找到。cscope使用符号交叉引用，默认叫做cscope.out，来定位文件中的函数，函数调用，宏，变量，预编译符号。
csh	和C的语法很象的shell csh是一个命令语言解释器，有历史功能，job控制工具，语法象C。
ctags	为Emacs，vi创建tag文件 etags使用emacs可以读取的格式创建tag表文件。ctags使用vi可以读取的格式创建tag表文件。
cut	剪切文件每一行的部分内容 输出每个文件每行的部分内容到stdout。
date	输出或设置系统日期和时间 以指定的格式显示当前时间，或设置系统时间。
df	报告文件系统的磁盘空间使用情况 显示文件所有文件系统的信息或所有的文件系统的信息。默认磁盘空间块大小为1K。
diff	一行行的比较文件 difference，
du	估计文件的空间使用 列出每个文件的磁盘使用情况，递归目录。
echo	显示一行文本 显示文本到stdout。
emacs	Emacs编辑器 因为Emacs的编辑命令使用Lisp编写，因此很容易扩充功能。CTRL-h查看帮助文档。CTRL-h t是一个交互式的教程教初学者Emacs的基础知识。
env	在一个更改后的环境中运行程序 设置环境中的每个NAME的值为VALUE，并运行COMMAND。
export	设置shell变量的export属性 每个NAME自动导出到后面执行的命令的环境中。如果提供了值，导出前赋值。
fg	在前台恢复程序 使job_spec成为当前job。如果没指定jobspec，将使用shell的当前job。
file	判断文件类型 测试每个参数来分类文件。输出的文件类型信息一般包含下面几个单词，"text"，文件只包含可打印字符和少量控制字符。"executable"，文件包含可以识别的编译后的程序。"data"，其它类型的文件，一般是二进制文件或不可打印的。
find	在目录中查找文件和目录 按给定的文件名、文件时间、文件大小在指定目录中查找文件和目录。
free	显示系统空闲和使用的内存 显示系统使用的物理内存和交换内存和空闲内存的数量，和核使用的缓存。
ftp	文件传输协议 ftp允许用户传输文件到远程网络地址，或从远程网络地址下载文件。
gcc	c和c++编译器 gcc预处理，编译，汇编，连接C/C++程序。"overall"选项可以在中间某个阶段停止处理。
gdb	"GNU调试器 gdb允许你查看当另一个程序运行时，内部正在做什么，或程序崩溃时程序正在做什么。GDB可以做4种事情以帮助你查找bug。
启动你的程序，指定可以影响程序行为的参数、选项。
让你的程序根据指定条件停止。
当你的程序停止时，检查发生了什么事。
更改你的程序，尝试更改bug。
在shell中通过命令启动gdb。一旦启动，它从终端读取命令，直到你输入quit命令退出GDB。"
grep	输出匹配PATTERN的行 grep在文件中查找PATTERN。
groups	输出用户所属的组 输出每个USERNAME所属的组，如果没指定USERNAME，则为当前进程。
gzip	压缩或解压文件 Gzip减少指定文件的大小。可能的话，每个文件被替换为扩展名为.gz的文件。
head	输出文件最前面的部分 输出每个文件的最前面的10行到stdout。如果有多个文件，输出前会输出文件名。如没指定文件或文件为-，则从stdin读取。
history	显示或操作命令历史列表 不指定选项，显示带行号的命令历史列表。参数n只列出最后的n个命令。!n执行history中的第n条命令。
hostname	输出或设置电脑名
info	"读取Info文档 Info文档被显示为一颗树，提供了快捷键在树节点和交叉链接上跳转。
q，quit,退出info程序。
h，help,显示info帮助窗口。
x，关闭info帮助窗口。
Space，向下翻一页。
Backspace，向上翻一页。
n，next,跳到同级别的下一节点。
p，previous,跳到同级别的上一节点。
u，up,跳到上一层节点。
t，top,跳到当前文档的顶层节点。
d，dir,跳到主目录节点。
l，回到上次看过的节点（历史节点）。
g，go to,跳到指定名字的节点。
[，跳到当前文档的上一节点。
]，跳到当前文档的下一节点。
0，选择当前节点的最后一个菜单项。
1..9，选择当前节点的第1到第9个菜单项。
m，menu,选择指定名字的菜单项。
r，选择指定名字的交叉链接。
Tab，光标跳到下一个链接。
Enter，进入光标所在处的链接。
Home，跳到当前节点的开始处。
End，跳到当前节点的结尾处。
s，search,查找指定的字符串。
{，查找前一个匹配。
}，查找后一个匹配。
Ctrl-g，取消当前操作。"
jobs	显示jobs的状态 列出活动的jobs。jobspec只输出这个job。
kill	发送信号到一个job 发送信号SIGSPEC或SIGNUM到以PID或JOBSPEC指定的进程。如果SIGSPEC和SIGNUM都没指定，则为SIGTERM。
killall	根据进程名字发送信号 killall发送一个信号到正在运行的指定命令的进程。如果没指定信号名，则发送SIGTERM。
last	列出最后登录用户的列表 lastb和last一样，除了默认它显示文件/var/log/btmp的日志，包含所有不成功的登录尝试。
ld	GNU连接器 ld合并多个obj文件和档案文件，重新定位它们的数据和绑定符号引用。通常编译程序的最后一步是运行ld。
less	和more相似 除了向前也允许往后翻页。less开始时不需要读取整个输入文件，因此比较快。命令基于more和vi。命令前可能会有一个数字，在下面的说明中称为N。
ln	在文件之间创建链接 link
locate	根据名称查找文件 locate读取一个或多个updatedb准备的数据库，并输出匹配PATTERNs的文件名到stdout，每行一个文件。PATTERNs可以包含通配符。locate不会检验从数据库中发现的文件是否存在。
ls	列出文件和目录的信息 list segment，默认情况下，以'.'开头的文件和目录不会被列出。
lsattr	文件属性列表 列出ext2的文件的属性。 -R，递归列出目录及文件的属性。&&&-V，显示程序版本。&&&-a，列出目录中所有的文件，包括以'.'开始的文件。&&&-d，象文件一样列出目录的属性，而不是列出它们的内容。&&&-v，列出文件的版本和世代数。
make	管理多个程序的工具 make的目的是决定一个大程序的哪些部分需要重新编译，执行相应的命令。使用make，必须写一个makefile文件来描述文件之间的关系。make执行makefile中的命令来更新一个或多个目标names，这里的name一般是一个程序。
man	"查看手册页 manual page，man使用less程序来查看手册页，功能键有'q'退出，'Enter'向下翻一行，'Space'向下翻一页，'b'向上翻一页，'/'后跟字符串再按'Enter'查找字符串，'n'查找下一个匹配。手册页分9部分，默认查找所有的部分。
1. 可执行程序或命令。
2. 系统调用（linux核提供的函数）。
3. 库调用（程序库提供的函数）。
4. 特殊文件（/dev目录下的文件）。
5. 文件格式和规范（如/etc/passwd）。
6. 游戏。
7. 其它。
8. 系统管理命令（仅root用户有用）。
9. 内核例程（非标准）。
象mkdir这种即是命令又是系统调用的，使用 man 1 mkdir查看mkdir命令的手册页，使用man 2 mkdir查看mkdir函数的手册页。"
mkdir	创建目录 如果不存在目录就创建目录。
mount	挂载文件系统 linux中所有可访问的文件被组织成一颗大树，即文件层次结构，/为根。mount命令将在某个设备上找到的文件系统加入这棵大树。mount的标准格式为 mount -t type device dir。这告诉核将在设备device(它的类型为type)上找到的文件系统加到文件系统的dir目录。
mv	移动文件 重命名文件SOURCE为DEST，或移动SOURCE文件到目录DIRECTORY。
nc	TCP/IP瑞士军刀 netcat是一个简单的工具，使用TCP或UDP协议从网络连接读写数据。是一个可靠的后端工具，可直接或容易被其它程序或脚本使用。同时它是一个功能丰富的调试和嗅探工具，它可以创建几乎任何种类的连接和几个有趣的内置功能。netcat也可以作为server使用，通过侦听任意端口的入站连接，读写数据。
od	以八进制和其它格式输出文件 以明确的表示，默认为八进制，输出文件到stdout。如果有多个文件参数，以列出的顺序串连一起组成输出。如果没指定文件或文件为-，从stdin读入。
passwd	更改用户密码 普通用户只能更改自己账号的密码，超级用户可以更改任何账号的密码。
perl	实用提取和报表语言 Perl可以读取任意长度的文本文件，从这些文件中提取信息，根据信息输出报表。也是一个好的系统管理语言。Perl能用复杂的模式匹配快速扫描大量数据。
php	PHP命令行接口'CLI' 可以通过使用参数-f后跟文件名解析和执行文件。使用参数-r可以直接执行PHP代码就象在.php文件中使用eval函数一样。
ping	发送ICMP ECHO_REQUEST到网络主机 ping发送ICMP协议的ECHO_REQUEST数据包给一个主机或网关，从主机或网关接收ECHO_RESPONSE。ECHO_REQUEST数据包(pings)有一个IP和ICMP头，加上一个timeval数据，和一个任意个数的填充(pad)字节。ping6也可以发送节点信息查询。
printenv	输出所有或部分环境 print environment，输出指定环境变量的值。如果没指定变量，输出环境中所有变量的名称和值。
ps	报告当前进程的信息 ps显示选择的活动进程的信息。如果想显示重复更新的进程信息，请使用top。
pwd	输出当前工作目录名 print working directory，输出当前工作目录的全文件名。
python	一种解释性、交互、面向对象的编程语言 可以自己用C或C++写模块来扩展Python，这些模块可以动态装入。可以通过运行pydoc程序来查看如何安装Python模块和包。
rename	更改多个文件的文件名 根据第一个参数指定的规则更改文件名。perlexpr是个正则表达式。如果命令行中没有指定文件名，将从stdin中读取文件名。如想去掉所有的匹配"*.bak"的文件的扩展，使用rename 's/\.bak$//' *.bak。将文件名大写变小写，使用rename 'y/A-Z/a-z/' *。
rm	删除文件或目录 rm删除指定的文件。默认不删除目录。
rmdir	删除空目录
set	设置或删除shell选项和位置参数的值 更改shell属性和位置参数的值或显示shell变量的名字和值。选项后的参数被当作位置参数的值，按顺序，分配给$1, $2, ...$n。
shutdown	关闭系统 以安全方式关闭系统。会通知所有登录用户系统将要关闭，禁用login。可以立即或指定的一段时间后关闭系统。第一时间会发送信号SIGTERM给所有的进程通知系统将要关闭，这会给象vi这样的程序保存正在编辑文件的时间。shutdown通知init进程要求更改运行级别。运行级别0用来暂停系统，级别6用来重启系统，级别1用来让系统进入一种状态，以便执行管理任务。
sort	对文本文件的行排序 输出所有文件排序好的内容到stdout。
source	从文件中读取和执行命令 BASH内置命令。从文件filename中读取命令，在当前shell环境执行并返回最后命令的退出状态。如果filename不包含/，在PATH中的目录中查找filename。当bash不在posix模式，如果在PATH中没找到，会在当前目录中查找。如果提供了arguments，filename执行时作为位置参数。
split	拆分文件为多块 将INPUT拆分PREFIXaa，PREFIXab，...等固定大小的多块。默认大小是1000行，默认PREFIX为x。如果没指定INPUT，或INPUT是-，读取stdin。
su	更改用户ID或变成超级用户 su用于在一个登录会话变成另一个用户。当前环境被传到新的shell。
tail	输出文件的最后部分 输出每个文件的最后面的10行到stdout。如果有多个文件，输出内容前会输出文件名。如没指定文件或文件为-，则从stdin读取。
tar	tar归档工具 tar在磁带或磁盘存储或提取文件。
`telnet	telnet客户端 telnet用来以telnet协议和另一主机通信。
top	显示linux任务 top提供动态实时的运行系统的视图。它可以显示系统的汇总信息和任务列表。
tty	输出终端的文件名
umount	卸载文件系统 umount使文件系统脱离文件层次结构。通过文件系统挂载的目录指定文件系统。文件系统忙时不能被卸载，如文件系统上有打开的文件。
uname	输出系统信息 输出某种系统信息。如果没有选项，同-s。
uptime	输出系统运行的时间 uptime输出一行，包括当前时间，系统运行的时间，当前登录用户的个数。
users	输出当前主机的登录用户的用户名 根据文件输出当前登录的用户。如果不指定文件，使用/var/run/utmp。通常指定文件为/var/log/wtmp。
vi	一个程序员的编辑器 Vim是向上兼容Vi的文本编辑器。可以编辑任何类型的普通文本。特别在编辑程序时有用。Vim运行时可以通过":help"命令来查看在线帮助。没有参数的选项可以合并后跟在一个-后。
w	显示登录用户和他们在做什么 w显示登录用户的和他们的进程。JCPU时间是tty上所有进程使用的时间，不包含已经完成的后台job，但包含正在运行的后台job。PCPU是当前进程使用的赶时间。
wc	输出每个文件的行数，单词和字节个数 输出每个文件的行数，单词和字节个数，和汇总行如果指定了多个文件。如果没指定文件或FILE为-，从stdin读取。
whatis	显示手册页描述 每个手册页中都有一个简单描述。whatis查找手册页名，并显示匹配name的所有手册页描述。
whereis	定位一个命令的文件、源程序、手册页 whereis定位指定文件的源程序/程序和手册页部分。先去掉指定名字的最前面的路径部分和最后的扩展名，如.c。whereis尝试在标准linux位置列表定位指定的程序
which	定位一个命令 which返回可以在当前环境下执行的文件(或链接)的路径。通过在PATH中查找匹配filename的执行文件。
who	输出当前登录的用户
whoami	输出用户名 输出当前有效用户ID关联的用户。
